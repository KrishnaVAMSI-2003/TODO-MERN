const API_URL = "https://todo-mern-api-7ki7.onrender.com";

const LOGIN_URL = `${API_URL}/auth/login`;
const SIGNUP_URL = `${API_URL}/auth/register`;
const alltodos = `${API_URL}/user/alltodos`;
const addtodo = `${API_URL}/user/addtodo`;
const updateTodo = `${API_URL}/user/updatetodo`;
const deleteTodo = `${API_URL}/user/deletetodo/`;

export { LOGIN_URL, SIGNUP_URL, alltodos, addtodo, updateTodo, deleteTodo };